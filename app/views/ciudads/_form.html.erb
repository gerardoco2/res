<%= form_with(model: ciudad, local: true) do |form| %>
  <% if ciudad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ciudad.errors.count, "error") %> prohibited this ciudad from being saved:</h2>

      <ul>
      <% ciudad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :ciudad_nombre %>
  </div>

  <div class="field">
    <%= form.label :codigo_area %>
    <%= form.text_field :codigo_area, id: :ciudad_codigo_area %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :ciudad_status %>
  </div>

  <div class="field">
    <%= form.label :estado_id %>
    <%= form.collection_select( :estado_id, Estado.all, :id, :nombre, {}) %>
    
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
